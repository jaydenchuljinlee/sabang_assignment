<!doctype html>
<html lang="en">
 <head>
  <meta charset="UTF-8">
  <meta name="Generator" content="EditPlus®">
  <meta name="Author" content="">
  <meta name="Keywords" content="">
  <meta name="Description" content="">
  <title>Document</title>

	<!--          -->
	<!-- CSS 시작 -->

	<!-- 부트스트랩 CSS -->
	<link rel="stylesheet" href="//unpkg.com/bootstrap@4/dist/css/bootstrap.min.css">
	<!-- style.css -->
	<link rel="stylesheet" href="/resources/css/style.css" />

	<!--          -->
	<!-- Javascript 시작 -->

	<!-- Jquery Javascript -->
	<script src='//unpkg.com/jquery@3/dist/jquery.min.js'></script>

	<!-- 부트스트랩 Javascript -->
	<script src='//unpkg.com/popper.js@1/dist/umd/popper.min.js'></script>
	<script src='//unpkg.com/bootstrap@4/dist/js/bootstrap.min.js'></script>

 </head>
 <body>

 	<?php
		session_start();
		
		if (!isset($_SESSION['identity'])) {
			echo "<script>alert('로그인을 해주세요');location.href='/';</script>";
		}
	?>
	<div class="contents" style="margin-top:200px;">
		<div class="container">
			<div class="user-list form-group col-12 mx-auto p-4 text-center" style="border: 1px solid #dee2e6;">
				<span class="font-weight-normal display-4">회원정보 리스트</span>
				<table class="table bolder table-hover col-8 mx-auto mt-5">
					<thead>
						<th>회원 ID</th>
						<th>회원 이름</th>
						<th>가입일</th>
						<th>수정일</th>
						<th>기타</th>
					</thead>
					<tbody>
						<?php
							
							#데이터 베이스 연결
							$conn = mysqli_connect(
								'localhost',
								'root',
								'cheoljin1234',
								'cheoljin'
							);

							$user_list	= "select user_id,user_name,created_date,updated_date,activation from user";
							$result		= mysqli_query($conn,$user_list);

							while($row = mysqli_fetch_array($result)) {
								echo "<tr>";
								echo "<td>".$row['user_id']."</td>";
								echo "<td>".$row['user_name']."</td>";
								echo "<td>".$row['created_date']."</td>";
								echo "<td>".$row['updated_date']."</td>";
								echo "<td>".$row['activation']."</td>";
								echo "</tr>";
							}
						?>
					</tbody>
				</table>
				<button id="user_update" class="list_btn next-page">정보수정</button>
				<button id="logout" class="list_btn next-page">로그아웃</button>
			</div>
		</div>
	</div>
 </body>
<form id="user_list_frm">
	<input name="state"></input>
</form>
	<!-- 실행에 필요한 자바스크립트 파일들 -->
	<!--                                   -->

	<script>
		
		$(document).ready(function() {
			
			$("#logout").on("click",function() {

				$("#user_list_frm").attr("action","/");
				console.log($("#user_list_frm").children());
				$("#user_list_frm").children().val("logout");
				
				$("#user_list_frm").submit();
			});
		});
	</script>
</html>
